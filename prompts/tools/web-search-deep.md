Search the public web. Pass {"query": "..."}. Optional recency: "any", "day", "week", "month", or "year". Returns a direct answer when available, then ranked titles, URLs, and snippets. Vary query wording across calls; keep useful quoted phrases. Then fetch_url promising pages.
