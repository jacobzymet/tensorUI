Search the public web. Pass {"query": "..."}. Optional recency: "any", "day", "week", "month", or "year". Returns a direct answer when available, then ranked titles, URLs, and snippets. Keep useful quoted phrases. You may call web_search several times in one turn with different queries; they run in parallel. Use fetch_url to read a specific page.
