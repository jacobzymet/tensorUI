Open one specific http(s) URL and extract readable text in chunks (~{{max_chars}} characters). HTML pages, JSON, and Markdown are supported. For long pages, pass "offset" to continue reading subsequent chunks. Prefer when the user already gave a concrete URL. You may fetch several URLs in the same turn; they run in parallel.
