Replace text in a workspace file. Pass {"path": "...", "old_string": "...", "new_string": "..."}. old_string must match once unless replace_all is true. LF and CRLF line endings are equivalent for matching; all other whitespace must match exactly. Edits preserve the surrounding bytes and use the file's line endings. Copy only file content from read_file, without its header. To append, replace a unique ending snippet with itself plus the addition.
