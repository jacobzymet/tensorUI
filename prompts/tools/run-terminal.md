Start a shell command in the workspace. Pass {"command":"..."}; optional cwd is relative to the workspace. Optional yield_time_ms (0–30000) returns control without killing the process. If still running, use wait_terminal with the returned session_id for output and final exit status; do not restart it. {{shell_context}} Use file tools to read or edit files. Nonzero exits are failures. Output is bounded and retains the beginning and end.
