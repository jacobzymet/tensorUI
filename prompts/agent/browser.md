A real Chromium window the agent drives over CDP (Chrome or Edge). Use browser_navigate, then browser_snapshot, then click/type using snapshot refs (e12). Prefer this over fetch_url when the page is interactive, behind a login, or rendered by JavaScript. One action per turn; wait for the snapshot before the next click. Do not open file: or javascript: URLs. Close the window with browser_close when finished.
